% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query-cells.R
\name{get_simulation_cells}
\alias{get_simulation_cells}
\title{Retrieve grid cells for a political/administrative unit, masked for crop presence}
\usage{
get_simulation_cells(
  con,
  resolution_arcmin,
  admin_level,
  admin_id = NULL,
  admin_name = NULL,
  crop = "cropland",
  min_frac_area = 0.05,
  mask_source = NULL
)
}
\arguments{
\item{con}{A DBI connection}

\item{resolution_arcmin}{Numeric resolution of the grid to query}

\item{admin_level}{Character, e.g. "ADM0", "ADM1", "customer_region"}

\item{admin_id}{Character admin_id to match (provide this OR admin_name)}

\item{admin_name}{Character admin_name to match (provide this OR admin_id)}

\item{crop}{Character crop label to filter on, default "cropland"}

\item{min_frac_area}{Numeric minimum crop area fraction to include a
cell, default 0.05. Set to 0 (or NULL to skip the crop join
entirely) for admin-only queries with no crop filter.}

\item{mask_source}{Character, restrict to a specific mask source/version.
If NULL, all matching mask_source rows are returned (will produce
duplicate cell_id rows if multiple sources are loaded -- specify
this explicitly once more than one source exists).}
}
\value{
sf object with cell_id, lon_center, lat_center, geometry, frac_area
}
\description{
This is the primary entry point for pulling a set of simulation-ready
cells: given an administrative unit (by ID or by name) and a minimum
crop area fraction, returns the matching grid cells with their
geometry and frac_area. No live geometry intersection happens here --
both the admin join and the crop mask join are pre-computed lookups
keyed on cell_id, so this is fast regardless of admin unit size.
}
