% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop-masks.R
\name{update_crop_mask}
\alias{update_crop_mask}
\title{Update the crop presence mask for cells in a grid table}
\usage{
update_crop_mask(
  con,
  raster_path,
  resolution_arcmin,
  crop = "cropland",
  mask_source,
  mask_year = NA_integer_,
  crop_class_values,
  admin_level = NULL,
  admin_id = NULL
)
}
\arguments{
\item{con}{A DBI connection}

\item{raster_path}{Path to a crop mask raster (e.g. ESA WorldCover, SPAM)}

\item{resolution_arcmin}{Numeric resolution of the grid table to aggregate onto}

\item{crop}{Character crop label, default "cropland" for a generic mask}

\item{mask_source}{Character label for provenance, e.g. "ESA_WorldCover_2021"}

\item{mask_year}{Integer year the mask represents, optional}

\item{crop_class_values}{Integer vector of raster values that count as
"cropland" / the crop of interest (depends on the source raster's
classification scheme -- check its legend)}

\item{admin_level, admin_id}{Optional: restrict aggregation to cells
within a given admin unit, to avoid processing a global raster when
only one country/region's cells exist in the grid table}
}
\value{
Invisibly, the number of rows inserted
}
\description{
Aggregates a crop mask raster onto an existing grid by computing,
per cell, the area-weighted fraction of that cell classified as
cropland (or a specific crop). Requires the 'terra' package.
}
