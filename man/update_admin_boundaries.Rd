% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin-boundaries.R
\name{update_admin_boundaries}
\alias{update_admin_boundaries}
\title{Ingest administrative boundaries into the cell_admin table}
\usage{
update_admin_boundaries(
  con,
  boundaries,
  admin_level,
  resolution_arcmin,
  source = NA_character_
)
}
\arguments{
\item{con}{A DBI connection}

\item{boundaries}{sf object with columns: admin_id, admin_name,
parent_id (may be NA), and geometry (WGS84)}

\item{admin_level}{Character label, e.g. "ADM0", "ADM1", "customer_region"}

\item{resolution_arcmin}{Numeric resolution of the grid table to join against}

\item{source}{Character label for provenance, e.g. "geoBoundaries", "GADM"}
}
\value{
Invisibly, the number of rows inserted
}
\description{
Joins a boundary sf object against an existing grid table (cell
centroid within boundary polygon) and writes the resulting
(cell_id, admin_level, admin_id) rows into masks.cell_admin.
}
\details{
This function is source-agnostic: pass in any sf object with the
required columns, regardless of whether it originated from GADM,
geoBoundaries, or a customer-drawn region upload.
}
