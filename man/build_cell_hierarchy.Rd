% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy.R
\name{build_cell_hierarchy}
\alias{build_cell_hierarchy}
\title{Build a fine-to-coarse cell hierarchy table}
\usage{
build_cell_hierarchy(con, fine_arcmin, coarse_arcmin)
}
\arguments{
\item{con}{A DBI connection}

\item{fine_arcmin}{Numeric resolution of the finer grid (smaller number)}

\item{coarse_arcmin}{Numeric resolution of the coarser grid (larger number)}
}
\value{
Invisibly, the hierarchy table name
}
\description{
Computes, for every cell in the fine-resolution grid table, which
coarse-resolution cell its centroid falls within, and writes the
result as a lookup table. This is computed once and never
recomputed -- it is what allows fast aggregation of fine-grid results
(e.g. soil-resolution simulation outputs) up to a coarser level
(e.g. weather-resolution) without repeated geometry operations.
}
