% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-write.R
\name{write_grid_to_postgis}
\alias{write_grid_to_postgis}
\title{Write a grid sf object to PostGIS as a new resolution table}
\usage{
write_grid_to_postgis(con, grid_sf)
}
\arguments{
\item{con}{A DBI connection, e.g. from get_db_connection()}

\item{grid_sf}{An sf object as returned by generate_grid_bbox() or
generate_grid_for_boundary()}
}
\value{
Invisibly, the table name written to
}
\description{
Creates the table (named via grid_table_name()), writes the cells,
and adds the standard spatial and lookup indexes. Uses upsert-style
behavior: if the table already exists, new cells are appended and
conflicting cell_ids are left untouched (existing rows are never
silently overwritten).
}
